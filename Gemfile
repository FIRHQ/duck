# encoding: utf-8

if ENV['USE_OFFICIAL_GEM_SOURCE']
  source 'https://rubygems.org'
else
  source 'https://gems.ruby-china.org'
end

gem 'faye'
gem 'sinatra'
gem 'redis'
gem 'redis-namespace'
gem 'thin'
gem 'rest-client'
